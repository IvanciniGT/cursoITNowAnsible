sudo apt update     # Actualkizar los repos
sudo apt install software-properties-common # Instalar un packete con dependencias
sudo add-apt-repository --yes --update ppa:ansible/ansible  # Añade el repo de ansible
sudo apt install ansible -y     # Instala ansible
